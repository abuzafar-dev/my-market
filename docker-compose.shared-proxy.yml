
services:
  frontend:
    networks:
      default: {}
      proxy:
        aliases:
          - my-market

networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:?Set PROXY_NETWORK in .env (name of the shared proxy network)}
