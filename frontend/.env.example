# Optional. By default the SPA calls https://<the page's own hostname>:8000/api,
# which works from localhost and from a phone on the LAN alike (see
# src/api/client.js). Set this in .env.local only to point somewhere else, e.g.:
# VITE_API_URL=https://api.example.com/api
